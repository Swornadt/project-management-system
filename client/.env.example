# Dev-only stopgap — see the comment block near the top of
# src/features/content/ContentDashboard.tsx for why these exist.
#
# There's no auth or live Projects/Users API yet, so the frontend has no way
# to know a real logged-in user or a real project to attach content to.
# Fill these in with real UUIDs from your dev database (a seeded project
# row and two different user rows — the reviewer must differ from the
# author, since the backend 403s self-approval).
#
# Remove this whole file once real auth + Projects/Users exist and replace
# these with actual session/context data.

VITE_DEV_PROJECT_ID=
VITE_DEV_AUTHOR_ID=
VITE_DEV_REVIEWER_ID=
